# 本次修复说明

保留 Flutter/Dart、现有页面布局与收银金额/折扣/舍入规则。修复分支为 `fix/pos-consistency-ocr`，本说明不代表已发布正式安装包。

## 更新后的使用方式

- 首次升级后，在原登录页选择管理员，给 `admin` 设置 6–12 位数字 PIN，并再次输入确认。原演示 PIN 不再作为密码。
- 管理员登录后，在原用户管理列表点选员工设置员工 PIN。角色从数据库账号读取，不能通过登录页选项提升权限。
- 先更新 PC，再更新手机。仍使用原扫码配对或 IP/端口连接方式。
- 手机销售、作废、商品/客户/供应商/分类修改、进货和盘点在已配对时写入持久待同步队列。电脑确认成功后才移除，断线时保留。
- 同一条操作重试不会重复入库或回补库存。原库存不足策略仍生效，选择“禁止”时结账会再次检查数据库实际库存。
- 手机与 PC 修改同一字段或盘点库存发生冲突时，停止该队列并保留手机数据，不自动覆盖任一方。原同步页面的错误提示会说明冲突对象；请核对实际数量并修正 PC 冲突值后再重试。不要通过恢复出厂设置清除待处理业务。
- 已有配对数据时不允许直接换成另一门店的配对令牌，以免混合门店数据；同一令牌的 IP 变更仍可保存。

## 历史数据边界

新销售会保存当时成本，之后商品改价不会改变其历史毛利。旧销售如果没有保存成本，无法凭空恢复历史进价，沿用已有估算方式。

新增作废回补记录防止重复操作；本次不会批量回补升级前已作废的旧单，以免把人工调整过的库存再次增加。

数据库通过增量迁移新增认证、ID 映射、待同步队列和幂等记录，不清空现有销售、商品或客户。

## 验证

各仓库的 `test/reliability_test.dart` 覆盖库存事务、作废回补、成本快照、并发结账、完整历史查询和实际 PIN 权限。

PC 仓库的 `integration` 独立测试项目使用真实本地 HTTP，验证两端不同 ID 关联、赊账客户、离线进货/销售/作废顺序、盘点冲突、断线重连、确认丢失重试和 PC 作废回传。GitHub Actions 的集成工作流固定手机配套提交。

最终分析、测试和打包是否成功，以对应提交的 GitHub Actions 结果为准。OCR 新功能尚未开始，需先读取用户指定的需求文档。
