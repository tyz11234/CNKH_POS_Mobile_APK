# 结账、取单及手机同步修复报告

日期：2026-09-20。本文记录上一轮已发布版本：Desktop 0.4.1+10、Mobile 1.10.2+30。当前 main 源码版本已统一为 Desktop 与 Mobile 1.10.3+31；本次 Desktop 增加 MyInvois 签名支持，Mobile 仅更新版本号，CI 验证中。

## 修复

1. 两端结账：通过 PopScope 和忙碌状态保护关闭、返回及重复操作；增加独立的提交完成回调，落库成功后立即清空所属购物车。找零读取不可变销售记录；已提交的页面不会再次生成销售，即使展示后续结果失败。
2. 两端取单：拒绝覆盖非空购物车，阻止并行取单；数据库删除必须确实消费一笔挂单，否则报告已被取出。原购物车的商品不再被无条件清空。
3. 手机进货：共用 PagedListFooter，修复 bottomNavigationBar 中无高度限制的 Center 撑满页面问题。
4. 手机图片：独立模块 ProductImageSyncQueue 使用 settings 持久保存按电脑地址隔离的图片任务，与商品目录在同一事务排队后再推进目录游标。先同步销售，再分批补图；下载错误和失败重试不丢任务。启用图片后首轮全量补查兼容升级前已推进的目录游标；失败任务轮转且等待 30 秒，防止阻塞其他图片。

## 数据与界面

数据库版本仍为 v9，无业务表迁移。没有修改 MyInvois、销售金额、税率、库存计算或 LAN 协议。新增图片任务仅保存在手机 settings 键中。保持原页面样式，只修复进货底栏高度并限制不安全操作。

## 新增回归覆盖

- 两端真实 SQLite 与 Widget：延迟结账期间关闭/返回被拦截，成功销售和扣库存各一次；强制移除页面后仍完成购物车处理；现金找零与保存金额一致。
- 两端挂单：非空购物车内容和原挂单均保留，正常取单成功，同一挂单第二次消费失败。
- 手机进货：52 笔记录，列表可见，分页在底部，第二页显示剩余 2 笔。
- 手机真实 localhost HTTP：图片返回 503 后目录游标仍前进，新同步客户端恢复并完成任务；关闭图片同步后开启会补查目录；图片成功后不重复下载。
- 手机队列：按电脑地址隔离，失败任务不饿死其他图片，删除图片状态取消过时下载任务。

发布前运行两端完整测试及真实 HTTP 配对回归。发布工作流重新分析、测试、捕获真实培训页面、构建并核验图片打包后，才上传安装包和 SHA256SUMS.txt；最终运行记录见对应 GitHub Actions 与 Release。

## 已知限制

尚未进行用户实际设备、打印机及门店 Wi-Fi 验收；MyInvois 仍沿用上一版功能和验收范围。手机沿用现有 debug 签名，签名冲突需先同步和备份。图片按每轮最多 4 张逐批处理，网络失败至少间隔 30 秒；大量图片不会一次全部出现。现有非致命静态分析告警不等于本次编译错误。
