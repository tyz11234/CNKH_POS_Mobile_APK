# CNKH POS Mobile 1.10.3+31

- 与 Desktop 版本统一为 1.10.3+31。
- Mobile 业务代码、数据库、离线销售与 LAN 同步协议未变更。

Mobile APK 构建、CI 及与 Desktop 的双端联调均已通过。APK 通过 `v1.10.3-mobile` Release 发布，包含 APK 和 SHA-256 校验文件。APK 沿用项目当前的 debug 签名配置；若 Android 提示签名不一致，请先完成数据同步和备份，再考虑卸载重装。
